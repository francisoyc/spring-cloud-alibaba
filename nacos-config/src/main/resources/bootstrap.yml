spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      config:
        server-addr: 192.168.2.101:8848
        file-extension: yaml
        prefix: nacos-config
      username: nacos
      password: nacos
      discovery:
        enabled: true
        #username: nacos
        #password: nacos
        server-addr: 192.168.2.101:8848

server:
  port: 8088

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
#nacos:
#  config:
#    password: nacos
#    username: nacos
#    server-addr: 192.168.2.101:8848